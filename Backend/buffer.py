buffer_memory = []

# Define a function to add a number to the buffer memory
def add_number_to_buffer(number):
    buffer_memory.append(number)

# Add numbers to the buffer memory
add_number_to_buffer(5)
add_number_to_buffer(10)
add_number_to_buffer(15)

# Print the buffer memory
print(buffer_memory)
